namespace KouCoCoa {
    public enum LogLevel {
        Error,
        Warning,
        Info,
        Debug
    }

    public enum RAthenaDbType {
        UNSUPPORTED,
        MOB_DB,
        MOB_AVAIL_DB,
        ITEM_DB
    }
}
