namespace KouCoCoa {
    public enum LogLevel {
        Error,
        Warning,
        Info,
        Debug,
        DebugVerbose
    }

    public enum RAthenaDbType {
        UNSUPPORTED,
        MOB_DB,
        MOB_AVAIL_DB,
        ITEM_DB,
        MOB_SKILLS_DB
    }
}
